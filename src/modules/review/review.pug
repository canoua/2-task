include review__header.pug
include review__content.pug

mixin review(source, name, date, icon, icontext, text)
  .review
    +review__header(source, name, date)
    +review__content(icon, icontext, text)